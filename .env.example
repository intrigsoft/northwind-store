# ── Northwind storefront ─────────────────────────────────────────────────────
# Copy to `.env.local` and fill in. The bare storefront needs no configuration —
# everything has a sensible default. The DioscHub assistant tutorial
# (see README) adds its variables in later steps.
